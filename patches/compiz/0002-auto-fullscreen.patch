diff --git a/src/privatewindow.h b/src/privatewindow.h
index 7509145..46ae163 100644
--- a/src/privatewindow.h
+++ b/src/privatewindow.h
@@ -294,6 +294,8 @@ class PrivateWindow :
 	bool resize (const CompWindow::Geometry &g);
 	bool resize (const XWindowAttributes &attrib);
 
+	void autoDetectFullscreen ();
+
 	void configure (XConfigureEvent *ce);
 
 	void configureFrame (XConfigureEvent *ce);
diff --git a/src/window.cpp b/src/window.cpp
index 10f0eea..7e3c0bc 100644
--- a/src/window.cpp
+++ b/src/window.cpp
@@ -1679,9 +1679,39 @@ PrivateWindow::resize (const CompWindow::Geometry &gm)
 	move (gm.x () - priv->geometry.x (),
 	      gm.y () - priv->geometry.y (), true);
 
+    autoDetectFullscreen ();
+
     return true;
 }
 
+void
+PrivateWindow::autoDetectFullscreen ()
+{
+    if (priv->attrib.override_redirect || !priv->managed)
+	return;
+
+    if (!(priv->wmType & (CompWindowTypeNormalMask | CompWindowTypeUnknownMask)))
+	return;
+
+    if (priv->state & CompWindowStateFullscreenMask)
+	return;
+
+    CompOutput &fsOutput = screen->fullscreenOutput ();
+
+    int fsX2 = fsOutput.x () + fsOutput.width ();
+    int fsY2 = fsOutput.y () + fsOutput.height ();
+    int wX2  = priv->geometry.x () + priv->geometry.width ();
+    int wY2  = priv->geometry.y () + priv->geometry.height ();
+
+    if (priv->geometry.x () <= fsOutput.x () &&
+	priv->geometry.y () <= fsOutput.y () &&
+	wX2 >= fsX2 && wY2 >= fsY2)
+    {
+	window->changeState (priv->state | CompWindowStateFullscreenMask);
+	window->updateAttributes (CompStackingUpdateModeNormal);
+    }
+}
+
 bool
 PrivateWindow::resize (const XWindowAttributes &attr)
 {
