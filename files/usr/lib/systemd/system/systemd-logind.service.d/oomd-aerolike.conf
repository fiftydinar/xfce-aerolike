[Service]
# Mark this cgroup as a last-resort oomd kill target (oomd ignores
# oom_score_adj; it only honors the oomd_avoid xattr).
# systemd-logind already ships OOMScoreAdjust=-1000, do not override.
ExecStartPost=-/usr/bin/oomd-protect-self
