# unbound chroot is disabled (chroot: "" in our config), so the
# TemporaryFileSystem and BindPaths directives intended for chroot
# mode interfere with direct file access to /etc/unbound certs.
# Remove them to fix Permission denied on TLS certificate files.
[Service]
TemporaryFileSystem=
BindReadOnlyPaths=
BindPaths=
