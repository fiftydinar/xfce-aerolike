[zram0]
zram-size=min(ram, 16384)
# lz4: fastest decode, lowest CPU cost per page fault. zstd's better
# ratio (fewer swap-ins) doesn't outweigh its per-fault decode cost on
# weak CPUs, where stutter under pressure is decode-latency bound.
compression-algorithm=lz4
