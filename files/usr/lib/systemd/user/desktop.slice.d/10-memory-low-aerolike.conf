# Reserve memory for the desktop environment scope (desktop.slice).
# The XFCE session runs inside a scope under this slice, so MemoryLow
# guarantees the DE can never be starved into swap by other apps.
# Relative to physical RAM (10%).
[Slice]
MemoryLow=10%
