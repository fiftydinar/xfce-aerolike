[Seat:*]
# Run the whole XFCE session inside a dedicated desktop.slice scope so
# the DE is a single protected cgroup unit (untargetable by oomd) and
# gets MemoryLow reservation. See /usr/bin/xfce-scoped-session.
session-wrapper=/usr/bin/xfce-scoped-session
